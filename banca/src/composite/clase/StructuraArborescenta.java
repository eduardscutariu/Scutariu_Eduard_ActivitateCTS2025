package composite.clase;

public interface StructuraArborescenta {
    void afiseazaStructuraArborescenta();
    void adaugaNod(StructuraArborescenta s);
    void stergeNod(StructuraArborescenta s);
    void getNodCopil(int index);
}
